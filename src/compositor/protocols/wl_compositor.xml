<?xml version="1.0" encoding="UTF-8"?>
<protocol name="wayland_compositor">
  <interface name="wl_compositor" version="4">
    <description summary="the compositor">
      A compositor.  This object is a factory for surfaces and region objects.
    </description>

    <request name="create_surface">
      <description summary="create a new surface">
        Ask the compositor to create a new surface.
      </description>
      <arg name="id" type="new_id" interface="wl_surface" />
    </request>

    <request name="create_region">
      <description summary="create a new region">
        Ask the compositor to create a new region.
      </description>
      <arg name="id" type="new_id" interface="wl_region" />
    </request>
  </interface>
</protocol>
